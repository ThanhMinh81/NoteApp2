<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Định dạng background của CheckBox khi không được chọn -->
    <solid android:color="@android:color/transparent" />
    <!-- Định dạng border của CheckBox khi không được chọn -->
    <stroke android:width="1dp" android:color="@color/backgroundItem" />
</shape>
